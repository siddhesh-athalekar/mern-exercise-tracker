import React from "react"

export default class CreateUser extends React.Component{
    render(){
        return(
            <div>
                You are in Create User component page
            </div>
        )
    }
}